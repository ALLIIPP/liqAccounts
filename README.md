# Swaps all the tokens in your SOL wallet to USDC


1. npm install
2. create .env file 
3. in .env fill in RPC_URL = '' // your rpc
4. in .env fill in PRIVATE_KEY = '' // private key of your wallet

5. node index.mjs